import { css } from 'styled-components'

const container = css`
	position: relative;
	border: 1px solid #ccc;
`

const input = css`
	border: none;
`

export const styles = {
	default: {
		container,
		input
	}
}

export default styles